# Breast-Cancer
A machine learning-based system in Python to detect breast cancer using the Wisconsin dataset. It applies algorithms like SVM and Random Forest to classify tumors as benign or malignant, using Scikit-learn, Pandas, and Matplotlib for model training, evaluation, and visualization.
